name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: logs a string
        run: echo "Hello world!"
      - name: runs multiple commands
        run: |
          node -v
          npm -v
      